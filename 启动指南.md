# 前端空白页问题排查指南

## 🔍 快速诊断

### 1. 检查浏览器控制台
- 按 F12 打开开发者工具
- 查看 Console 标签页的错误信息
- 将错误信息复制给我

### 2. 检查网络请求
- 在 Network 标签页查看是否有红色请求
- 检查是否有 404 或 500 错误

### 3. 临时诊断页面
访问 `http://localhost:3000/diagnose.html` 查看诊断信息

## 🛠️ 快速修复步骤

### 步骤 1：清除浏览器缓存和本地存储
```javascript
// 在浏览器控制台 (F12) 中执行：
localStorage.clear();
sessionStorage.clear();
location.reload();
```

### 步骤 2：重新安装依赖
```bash
cd /Users/fightingatm/Downloads/ai_learner/ai-learning-helper-frontend
rm -rf node_modules package-lock.json
npm install
```

### 步骤 3：启动前端
```bash
npm run dev
```

## 📋 常见问题及解决方案

### 问题 1：API 请求失败
**症状**：页面空白，控制台显示网络错误

**解决方案**：
1. 确保后端已启动：`cd ai-learning-helper-backend && mvn spring-boot:run`
2. 等待后端完全启动（约 10-20 秒）
3. 检查后端是否在 8080 端口运行

### 问题 2：React 渲染错误
**症状**：控制台显示 JavaScript 错误

**解决方案**：
1. 检查是否所有依赖都已正确安装
2. 尝试删除 node_modules 并重新安装

### 问题 3：路由问题
**症状**：页面加载但内容为空

**解决方案**：
1. 检查 App.tsx 中的路由配置
2. 确保所有页面组件都正确导入

## 🚀 完整启动流程

### 1. 启动数据库（确保 MySQL 运行）
```bash
# 检查 MySQL 状态
mysql.server status

# 如果未运行，启动 MySQL
mysql.server start
```

### 2. 执行数据库迁移
```bash
cd /Users/fightingatm/Downloads/ai_learner
mysql -u root -p < migrate_safe.sql
```

### 3. 启动后端
```bash
cd /Users/fightingatm/Downloads/ai_learner/ai-learning-helper-backend
mvn spring-boot:run
```
等待看到 "Started AiLearningHelperApplication" 消息

### 4. 启动前端（新终端窗口）
```bash
cd /Users/fightingatm/Downloads/ai_learner/ai-learning-helper-frontend
npm install  # 如果还没安装依赖
npm run dev
```

### 5. 访问应用
打开浏览器访问：`http://localhost:3000`

## 📞 需要提供的信息

如果以上步骤都无法解决，请提供：
1. 浏览器控制台的完整错误信息
2. Network 标签页的截图或错误信息
3. 后端启动日志的最后 20 行

## 🔧 紧急修复：创建最简单的测试页面

如果以上都不起作用，可以先创建一个最简单的测试页面来验证基本功能：
